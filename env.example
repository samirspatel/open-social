# GitHub Pages Configuration
# No environment variables needed - GitSocial runs entirely client-side!

# Application Environment
NODE_ENV=development

# Security Configuration (optional - for development)
ALLOWED_COLLABORATORS=samirpatel
RATE_LIMIT_PER_HOUR=100

# Note: GitSocial uses Personal Access Tokens for authentication
# No OAuth setup required - perfect for GitHub Pages deployment!
